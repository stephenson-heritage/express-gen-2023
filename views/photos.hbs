<div class="gallery">
	{{#each this}}
		{{#if filename}}
			<div class="gallery-photo">
				<a href="./photo/{{photo_id}}">
					<img src="/images/gallery/{{filename}}" />
				</a>
				<p>{{description}}</p>

			</div>
		{{/if}}
	{{/each}}
</div>